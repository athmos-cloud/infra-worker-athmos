tmpDir: "/tmp/worker"
test:
  credentials:
    gcp: "/etc/credentials/resource.json"
http:
  port: 8081
kubernetes:
  configPath: "/root/.kube/config"
queue:
  uri: "amqp://user:password@rabbitmq:5672/"
  exchange: "to-gateway-api"
  queue: "to-infra-worker"
mongo:
  address: "mongodb"
  port: 27017
  username: "admin"
  password: "password"
  database: "plugin-db"
  projectCollection: "projects"
