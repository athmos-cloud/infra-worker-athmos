tmp_dir: "/tmp/worker"
plugins:
  crossplane:
    registry:
      address: "http://chart-museum:8080"
      username: "athmos"
      password: "athmos"
    gcp:
      firewall:
        chart: "gcp-firewall"
        version: "0.1.0"
      network:
        chart: "gcp-network"
        version: "0.1.0"
      provider:
        chart: "gcp-provider"
        version: "0.1.0"
      subnet:
        chart: "gcp-subnetwork"
        version: "0.1.0"
      vm:
        chart: "gcp-vm"
        version: "0.1.0"
      vpc:
        chart: "gcp-vpc"
        version: "0.1.0"
minio:
  address: "minio:9000"
  access_key_id: ""
  secret_access_key: ""
  use_ssl: false
mongo:
  address: "mongo"
  port: 27017
  username: "root"
  password: "password"
  database: "plugin-db"
postgres:
  address: "postgres"
  port: 5432
  username: "postgres"
  password: "password"
  database: "plugin-db"


