- name: identifier
  fields:
    providerID:
      type: string
      displayName: "Provider"
      description: "The name of the provider to use"
      required: true
    vpcID:
      type: string
      displayName: "Name"
      description: "The name of the VPC"
      required: true